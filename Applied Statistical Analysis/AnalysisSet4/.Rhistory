od <- exp(coef(mult.log))
# remove objects
rm(list=ls())
# detach all libraries
detachAllPackages <- function() {
basic.packages <- c("package:stats", "package:graphics", "package:grDevices", "package:utils", "package:datasets", "package:methods", "package:base")
package.list <- search()[ifelse(unlist(gregexpr("package:", search()))==1, TRUE, FALSE)]
package.list <- setdiff(package.list, basic.packages)
if (length(package.list)>0)  for (package in package.list) detach(package,  character.only=TRUE)
}
detachAllPackages()
# load libraries
pkgTest <- function(pkg){
new.pkg <- pkg[!(pkg %in% installed.packages()[,  "Package"])]
if (length(new.pkg))
install.packages(new.pkg,  dependencies = TRUE)
sapply(pkg,  require,  character.only = TRUE)
}
lapply(c("nnet", "MASS"),  pkgTest)
# set wd for current folder
setwd(dirname(rstudioapi::getActiveDocumentContext()$path))
# load data
gdp_data <- read.csv("https://raw.githubusercontent.com/ASDS-TCD/StatsII_2026/main/datasets/gdpChange.csv", stringsAsFactors = F)
#First we must categorize the GDPWdiff column and then factor it.
gdp_data$GDPWdiff_cat <- ifelse(gdp_data$GDPWdiff > 0, "Positive",
ifelse(gdp_data$GDPWdiff == 0, "No Change", "Negative"))
gdp_data$GDPWdiff_cat <- factor(gdp_data$GDPWdiff_cat)
#set the reference category
gdp_data$GDPWdiff_cat <- relevel(gdp_data$GDPWdiff_cat, ref = "No Change")
# run model
mult.log <- multinom(GDPWdiff_cat ~ REG + OIL, data = gdp_data)
summary(mult.log)
od <- exp(coef(mult.log))
stargazer(od)
stargazer
library(stargazer)
od <- exp(coef(mult.log))
stargazer(od)
# remove objects
rm(list=ls())
# detach all libraries
detachAllPackages <- function() {
basic.packages <- c("package:stats", "package:graphics", "package:grDevices", "package:utils", "package:datasets", "package:methods", "package:base")
package.list <- search()[ifelse(unlist(gregexpr("package:", search()))==1, TRUE, FALSE)]
package.list <- setdiff(package.list, basic.packages)
if (length(package.list)>0)  for (package in package.list) detach(package,  character.only=TRUE)
}
detachAllPackages()
# load libraries
pkgTest <- function(pkg){
new.pkg <- pkg[!(pkg %in% installed.packages()[,  "Package"])]
if (length(new.pkg))
install.packages(new.pkg,  dependencies = TRUE)
sapply(pkg,  require,  character.only = TRUE)
}
lapply(c("nnet", "MASS"),  pkgTest)
# load data
mexico_elections <- read.csv("https://raw.githubusercontent.com/ASDS-TCD/StatsII_2026/main/datasets/MexicoMuniData.csv")
str(mexico_elections)
long_data <- read.table("http://statmath.wu.ac.at/courses/StatsWithR/Long.txt", header=T)
# Make sure your data are in the correct format.
str(long_data)
#Examining the data
plot(mexico_elections$competitive.district, mexico_elections$PAN.visits.06)
ggplot(mexico_elections, aes(ment, art, color = fem)) +
geom_jitter(alpha = 0.3)
library(ggplot2)
#Examining the data
plot(mexico_elections$competitive.district, mexico_elections$PAN.visits.06)
ggplot(mexico_elections, aes(ment, art, color = fem)) +
geom_jitter(alpha = 0.3)
# Make sure your data are in the correct format.
str(long_data)
ggplot(mexico_elections, aes(competitive.district, PAN.visits.06, color = fem)) +
geom_jitter(alpha = 0.3)
ggplot(mexico_elections, aes(competitive.district, PAN.visits.06, color = marginality.06)) +
geom_jitter(alpha = 0.3)
#Examining the data
summary(mexico_elections$PAN.visits.06)
table(mexico_elections$PAN.visits.06)
hist(mexico_elections$PAN.visits.06, breaks = 20)
mean(mexico_elections$PAN.visits.06)
var(mexico_elections$PAN.visits.06)
#Running the Model
pois_mod <- glm(PAN.visits.06 ~ competitive.district + marginality + PAN.governor.06,
data = mexico_elections,
family = poisson(link = "log"))
#Running the Model
pois_mod <- glm(PAN.visits.06 ~ competitive.district + marginality.06 + PAN.governor.06,
data = mexico_elections,
family = poisson(link = "log"))
summary(pois_mod)
new.pkg <- pkg[!(pkg %in% installed.packages()[,  "Package"])]
if (length(new.pkg))
install.packages(new.pkg,  dependencies = TRUE)
sapply(pkg,  require,  character.only = TRUE)
# load libraries
pkgTest <- function(pkg){
new.pkg <- pkg[!(pkg %in% installed.packages()[,  "Package"])]
if (length(new.pkg))
install.packages(new.pkg,  dependencies = TRUE)
sapply(pkg,  require,  character.only = TRUE)
}
lapply(c("nnet", "MASS", "stargazer"),  pkgTest)
# set wd for current folder
setwd(dirname(rstudioapi::getActiveDocumentContext()$path))
stargazer(pois_mod)
#Predicted Values
newdata <- data.frame(
competitive.district = 1,
marginality.06 = 0,
PAN.governor = 1,
)
#Predicted Values
newdata <- data.frame(
competitive.district = 1,
marginality.06 = 0,
PAN.governor = 1
)
predict(pois_mod, newdata, type = "response")
coeff[1]
#Predicted Values
newdata <- data.frame(
competitive.district = 1,
marginality.06 = 0,
PAN.governor = 1
)
predict(pois_mod, newdata, type = "response")
# What is the predicted number of articles for a married male PhD researcher with 1 child at 2-rated institute whose PhD supervisor published 5 articles?
newdata <- data.frame(
fem = 0,
mar = 1,
kid5 = 1,
phd = 2,
ment = 5
)
predict(pois_mod, newdata, type = "response")
# Plot predictions vs count.
pred <- predict(pois_mod, type = "response")
plot(long_data$art, pred,
xlab = "Observed count (art)",
ylab = "Predicted count",
main = "Predicted vs Observed Counts")
pred <- predict(pois_mod, type = "response")
pred
# What is the predicted number of articles for a married male PhD researcher with 1 child at 2-rated institute whose PhD supervisor published 5 articles?
newdata <- data.frame(
fem = 0,
mar = 1,
kid5 = 1,
phd = 2,
ment = 5
)
predict(pois_mod, newdata, type = "response")
# (b) Perform a Poisson regression of number of articles published on the explanatory variables.
pois_mod <- glm(art ~ fem + mar + kid5 + phd + ment,
data = long_data,
family = poisson(link = "log"))
summary(pois_mod)
# Do we meet assumptions for Poisson?
mean(long_data$art)
var(long_data$art)
#plotting
coeffs <- coefficients(pois_mod)
xvalues <- sort(long_data$ment)
means <- exp(coeffs[1] + coeffs[6]*xvalues)
plot(long_data$ment, long_data$art)
lines(xvalues, means, lty=2, col="red")
# What is the predicted number of articles for a married male PhD researcher with 1 child at 2-rated institute whose PhD supervisor published 5 articles?
newdata <- data.frame(
fem = 0,
mar = 1,
kid5 = 1,
phd = 2,
ment = 5
)
predict(pois_mod, newdata, type = "response")
# Plot predictions vs count.
pred <- predict(pois_mod, type = "response")
plot(long_data$art, pred,
xlab = "Observed count (art)",
ylab = "Predicted count",
main = "Predicted vs Observed Counts")
# remove objects
rm(list=ls())
# detach all libraries
detachAllPackages <- function() {
basic.packages <- c("package:stats", "package:graphics", "package:grDevices", "package:utils", "package:datasets", "package:methods", "package:base")
package.list <- search()[ifelse(unlist(gregexpr("package:", search()))==1, TRUE, FALSE)]
package.list <- setdiff(package.list, basic.packages)
if (length(package.list)>0)  for (package in package.list) detach(package,  character.only=TRUE)
}
detachAllPackages()
# load libraries
pkgTest <- function(pkg){
new.pkg <- pkg[!(pkg %in% installed.packages()[,  "Package"])]
if (length(new.pkg))
install.packages(new.pkg,  dependencies = TRUE)
sapply(pkg,  require,  character.only = TRUE)
}
lapply(c("nnet", "MASS", "stargazer"),  pkgTest)
# set wd for current folder
setwd(dirname(rstudioapi::getActiveDocumentContext()$path))
# load data
gdp_data <- read.csv("https://raw.githubusercontent.com/ASDS-TCD/StatsII_2026/main/datasets/gdpChange.csv", stringsAsFactors = F)
#First we must categorize the GDPWdiff column and then factor it.
gdp_data$GDPWdiff_cat <- ifelse(gdp_data$GDPWdiff > 0, "Positive",
ifelse(gdp_data$GDPWdiff == 0, "No Change", "Negative"))
gdp_data$GDPWdiff_cat <- factor(gdp_data$GDPWdiff_cat)
#set the reference category
gdp_data$GDPWdiff_cat <- relevel(gdp_data$GDPWdiff_cat, ref = "No Change")
# run model
mult.log <- multinom(GDPWdiff_cat ~ REG + OIL, data = gdp_data)
summary(mult.log)
od <- exp(coef(mult.log))
stargazer(mult.log)
stargazer(od)
orci <- exp(cbind(OR = coef(mult.log), confint(mult.log)))
ord.log <- polr(GDPWdiff_cat ~ REG + OIL, data = gdp_data, Hess = TRUE)
summary(ord.log)
stargazer(ord.log)
ctable <- coef(summary(ord.log))
p <- pnorm(abs(ctable[, "t value"]), lower.tail = FALSE) * 2
(ctable <- cbind(ctable, "p value" = p))
# Calculate confidence intervals
(ci <- confint(ord.log))
# Convert to odds ratio
or <- exp(cbind(OR = coef(ord.log), ci))
# load data
mexico_elections <- read.csv("https://raw.githubusercontent.com/ASDS-TCD/StatsII_2026/main/datasets/MexicoMuniData.csv")
#Running the Model
pois_mod <- glm(PAN.visits.06 ~ competitive.district + marginality.06 + PAN.governor.06,
data = mexico_elections,
family = poisson(link = "log"))
summary(pois_mod)
stargazer(pois_mod)
#Predicted Values
newdata <- data.frame(
competitive.district = 1,
marginality.06 = 0,
PAN.governor = 1
)
predict(pois_mod, newdata, type = "response")
#Predicted Values
newdata <- data.frame(
competitive.district = 1,
marginality.06 = 0,
PAN.governor.06 = 1
)
predict(pois_mod, newdata, type = "response")
#Predicted Values
newdata <- data.frame(
competitive.district = 1,
marginality.06 = 0,
PAN.governor.06 = 1
)
predict(pois_mod, newdata, type = "response")
lb <- exp(-3.810 - (0.081*1) - (3.810*1))
lb
exp(0.01494818)
lb <- exp(-3.810 - 0.081(1) - 3.810(1))
lb <- exp(-3.810 - (0.081*1) - (2.080*0) - (3.810*1))
lb
#Predicted Values
newdata <- data.frame(
competitive.district = 1,
marginality.06 = 0,
PAN.governor.06 = 1
)
predict(pois_mod, newdata, type = "response")
lb <- exp(-3.810 - (0.081*1) - (2.080*0) - (3.12*1))
lb
stargazer(pois_mod)
lb <- exp(-3.810 - 0.081 - 3.12)
lb
#Predicted Values
newdata <- data.frame(
competitive.district = 1,
marginality.06 = 0,
PAN.governor.06 = 1
)
predict(pois_mod, newdata, type = "response")
#Predicted Values
newdata <- data.frame(
competitive.district = 1,
marginality.06 = 0,
PAN.governor.06 = 1
)
predict(pois_mod, newdata, type = "response")
lb <- exp(-3.810 - 0.081 - 3.12)
lb
lb <- exp(-3.810 - 0.081 - 0.312)
lb
